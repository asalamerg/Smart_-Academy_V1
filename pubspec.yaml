name: smart_academy
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# Version and build number
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.0
  firebase_storage: ^12.4.5 # Update to a compatible version
  cloud_firestore: ^5.5.0
  firebase_auth: ^5.3.3
  url_launcher: ^6.2.5
  loading_indicator: ^3.1.1
  flutter_slidable: ^3.1.2
  fluttertoast: ^8.2.12
  flutter_bloc: ^9.1.0
  file_picker: ^6.1.1
  image_picker: ^0.8.5+3

  intl: ^0.18.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.4.0
  flutter_launcher_icons: ^0.14.3
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/image/

  # Add custom fonts if needed:
  # fonts:
  #   - family: Kavoon
  #     fonts:
  #       - asset: assets/font/Kavoon-Regular.ttf
